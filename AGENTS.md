# AGENTS.md — eval-stu 운영 지침 (SSOT)

이 문서는 `eval-stu` 저장소의 **유일한 규칙 원천(SSOT)**입니다. **학생 평가(과제 수집·채점·성적 관리)** 자동화 저장소 운영 지침을 담습니다.

> **⚠️ SSOT 경고**: `.agents/rules/language.md`와 `.agents/rules/rules-project.md`는 **생성 금지**입니다. 모든 규칙은 이 문서(AGENTS.md)에만 정의됩니다. pre-commit 훅이 재생성을 차단합니다.
>
> **`.claude/` 디렉터리 생성 금지**: 이 프로젝트는 `.agents/`만 사용합니다. `.claude/`, `.claude/settings.json`, `.claude/commands/` 등 Claude Code 전용 구성 파일·디렉터리를 생성하지 않습니다.

- 스킬: `.agents/skills/<name>/SKILL.md`
- 워크플로우: `.agents/workflows/*.md`
- 프로젝트 개요 및 디렉터리 구조는 `README.md` 참고.

---

## 📚 2026-2학기 개설 강좌 및 운영 분반

**분반 및 과목 동의어 매핑 (Course Synonyms)**

| 대표 명칭 | 동의어 (호환 표기) | 대상 강좌 및 분반 상세 |
|:---:|---|---|
| **1반** | `01`, `web1`, `웹1`, `ICE0001-01`, `761`, `15143` | E트랙 `ICE0001-01` 웹프로그래밍(E트랙) 01분반 (월 16:00\~18:45, 7-9교시) |
| **2반** | `02`, `web2`, `웹2`, `ICE0001-02`, `762`, `15144` | E트랙 `ICE0001-02` 웹프로그래밍(E트랙) 02분반 (월 13:00\~15:50, 5-7교시) |
| **4반** | `04`, `python`, `파이썬`, `GPU0940-04`, `468`, `14712` | K트랙 `GPU0940-04` 컴퓨팅적사고와코딩(K트랙) (월 09:00\~11:50, 1-3교시) |

> **과목 호칭 원칙**: 모든 과목은 대표 명칭(**1반**, **2반**, **4반**) 및 정의된 동의어를 공식 동의어로 취급하며 상호 완벽히 호환하여 인식하고 사용한다.

| 대표 명칭 | 동의어 (호환 표기) | 트랙 / 분반 | 학과명 | 교과목번호 (강좌ID) | 교과목명 | 학점 | 수업시간 | 강의실 | 수강 인원 | 언어 |
|:---:|---|:---:|---|:---:|---|:---:|---|---|:---:|:---:|
| **1반** | `01`, `web1`, `웹1`, `ICE0001-01`, `761`, `15143` | E트랙 01분반 | 컴퓨터공학과(국) | **ICE0001-01** (15143) | 웹프로그래밍(E트랙) | 3 | 월(16:00\~18:45, 7-9교시) | 본관 IT융합소프트웨어학과 실습실 (3층 308호) | 31 | 영어 |
| **2반** | `02`, `web2`, `웹2`, `ICE0001-02`, `762`, `15144` | E트랙 02분반 | 컴퓨터공학과(국) | **ICE0001-02** (15144) | 웹프로그래밍(E트랙) | 3 | 월(13:00\~15:50, 5-7교시) | 본관 IT융합소프트웨어학과 실습실 (3층 308호) | 32 | 영어 |
| **4반** | `04`, `python`, `파이썬`, `GPU0940-04`, `468`, `14712` | K트랙 | 교양교육원 | **GPU0940-04** (14712) | 컴퓨팅적사고와코딩(K트랙) | 3 | 월(09:00\~11:50, 1-3교시) | 본관 컴퓨터실습실 (2층 205호) | 14 (수강 9 + 참여 5) | 한국어 |

---

## 🌐 플리토(Flitto Live TR) 실시간 통번역 시스템

외국인 유학생(베트남어, 네팔어, 미얀마어 등)의 수업 지원을 위해 실시간 번역 서비스를 운영합니다.

- **접속 링크**: [Flitto Live TR](https://livetr.flit.to/login)
- **로그인 정보**:
  - 아이디: `stu02.host`
  - 비밀번호: `Stu2026!@#`
- **방 입장**:
  - 4반 (파이썬): `정원혁 / 월 1-3교시 / 컴퓨팅적사고와코딩(K트랙)`
  - 1반 / 2반 (웹): `정원혁 / 월 5-10교시 웹프로그래밍(E트랙)`
- **마이크 수령/반납**: 국제학부 사무실 (우석 1층, 032-340-9578)

---

## 📊 성적 및 학생 관리 (Grade Management)


- **구글 스프레드시트 SSOT**: [`1docs/sheets.md`](file:///home/hyuk/prj/stu/eval-stu/1docs/sheets.md) *(4개 시트 ID, URL, 탭 구조, 시트 언어 정책의 유일한 원천)*
  - 전체 학생 명부 (비공개, 교수 전용)
  - 파이썬 4반 성적부 (`score`, `grade`)
  - 웹프로그래밍 1반 (web1) 성적부 (100% 영어 기록)
  - 웹프로그래밍 2반 (web2) 성적부 (100% 영어 기록)
- **학생 명단 SSOT**: 별도 저장소인 **`../stu2603/5input/students/`가 유일한 단일 규칙 원천(SSOT)**입니다. 이 저장소(`eval-stu`)의 `5input/students`는 상대 심볼릭 링크(`../../stu2603/5input/students`)로 연결되어 읽기 전용으로 참조합니다(`py-students.md`, `wb-students.md`, `students_summary.md`). **`eval-stu` 내부에서 학생 명단을 직접 생성하거나 수정하지 마십시오.** 학생 추가·수정·eCampus 명단 갱신은 항상 `stu2603`에서 수행합니다.
- **과제 이메일(0.x) 채점 정책 SSOT**: **[`1docs/score-email.md`](file:///home/hyuk/prj/stu/eval-stu/1docs/score-email.md)**
  - 마이크로 과제의 모든 채점 원칙(배점 티어, 유예시간, 순수 본문 검증, 감점 기준, 다중 발송 멱등성, 시트 데이터 포맷팅 등)은 `1docs/score-email.md`가 **단일 규칙 원천(SSOT)**입니다.
  - **핵심 요약**:
    - **단일 규격**: 모든 마이크로 과제는 오직 `0.x` 규격으로만 운영 (`과제1` 등 다른 명칭 없음)
    - **마감 및 유예**: 매주 목 23:59 KST (+15분 유예, 금 00:15까지 정상 인정). 지각은 **다음 수업 시작 시각 전까지** 인정 (4반: 월 09:00 전, 2반: 월 13:00 전, 1반: 월 16:00 전), 이후는 불인정(0점)
    - **배점 티어 (1.0점 만점)**: 1.0(정상) / 0.9(관용/경미오차) / 0.7(형식미흡) / 0.5(지각) / 0.2(지각+형식미흡) / 0.0(미제출/불인정/지각초과)
    - **미제출자/0점 시트 등록**: 0.0점(미제출·불인정)도 시트에서 제외하지 않고 0점으로 등록 (`Reason`: `No submission` / `미제출` 등)
    - **순수 본문 검증**: 인용문 제외 30자 이상 또는 유효 URL 필수 (빈 회신은 0점 불인정)
    - **시트 정렬 규칙 (4단계)**: 1차 `wk` 역순 → 2차 `Type1` 오름차순 → 3차 `Type2` 오름차순 → 4차 `ID` 오름차순 (일련번호 `No`는 최상단부터 아래로 `N` down to `1` 내림차순 순차 부여)
- **배포 및 공유 자료 언어 정책 (Distribution & Sheet Language Policy)**:
  - 학생들에게 배포·공유되는 모든 자료(스프레드시트, 과제 피드백, 안내문 등)는 해당 트랙의 강의 언어와 100% 일치하도록 작성한다.
  - **E트랙 (영어 트랙: 1반 / 2반 웹프로그래밍)**: 헤더뿐만 아니라 **Reason(사유), Type1, Type2, 상태 등 모든 셀의 내용과 텍스트를 예외 없이 100% 영어**로 작성한다. (`No`, `wk`, `ID`, `Track`, `Score`, `Type1`, `Type2`, `Reason`, `Date`, `Name`, `Subject`)
  - **K트랙 (한국어 트랙: 4반 파이썬)**: 헤더뿐만 아니라 **이유(사유), 상태 등 모든 셀의 내용과 텍스트를 예외 없이 100% 한국어**로 작성한다. (`no`, `주차`, `학번`, `트랙`, `점수`, `유형1`, `유형2`, `이유`, `날짜`, `이름`, `메일제목`)

---

## 🔗 형제 저장소와의 경계

### 강의안 저장소 (`../stu2603`)

- 강의안·과제 SSOT는 별도 저장소 `../stu2603`입니다. 이 저장소에서는 **읽기만** 하고, 수정은 stu2603 쪽에서 합니다.
- 교차 참조는 심볼릭 링크로 연결: `1docs/sheets.md`, `1docs/assignment-micro.md`, `1docs/scores.md`, `5input/py`, `5input/web`, `5input/students`
- 교차 링크는 **항상 상대경로**로 만듭니다(마운트 경로 변경에도 살아남도록). 깨진 링크는 pre-commit 훅이 차단합니다.

### 과목별 교재 폴더 (`../py2608-stu`, `../web2608-stu`)

- `../py2608-stu` — 파이썬 4반 (K트랙) 교재·과제 자료
- `../web2608-stu` — 웹 1반/2반 (E트랙) 교재·과제 자료
- 이 저장소(`eval-stu`)에서 **채점·성적 기록·이메일 답장 발송**을 수행합니다.

---

## ⚠️ 핵심 제약 조건

- **자격증명 파일 접근 금지**: `secret.json`, `credentials.json`, `token.json`, `service-account.json`은 읽지도, 출력하지도, 커밋하지도 않습니다.
- **학생 개인정보 보호**: 성적 CSV/TSV, 학생 사진, 이메일 본문은 git에 추가하지 않습니다 (`output/`, `9output/`, `*.csv`, `*.tsv`는 gitignore 유지).
- **파이썬 코드 검증**: `.py` 수정 후에는 `./bin/harness-check.sh`(Black·Flake8·Mypy·Pytest)를 통과해야 하며, pre-commit 훅이 강제합니다.
- **크롬 자동화 시 메인 브라우저 무간섭 및 전용 프로필(`~/.config/eval-stu-grader`) 운영 원칙**: 사용자의 일상 업무용 메인 크롬(`~/.config/google-chrome`)은 **절대 종료하지도, 복사하지도, 접근하지도 않습니다(Zero-Interference)**. 자동화는 오직 독립된 고정 전용 디렉터리(`~/.config/eval-stu-grader`)의 단일 `Default` 프로필만을 백그라운드로 띄워 실행하며, 채점 종료 시 해당 전용 프로세스만 종료합니다. 프로필 번호 증식 및 메인 크롬 세션 만료를 원천 차단합니다. (상세 설계: `1docs/email-grader-plan.md` 참고)

---

## AI 행동 규칙 (에이전트 전용)

**1. 사전 확인 (작업 시작 전 필독)**

작업 시작 전 이 저장소 루트의 `AGENTS.md`를 반드시 읽는다. 모든 규칙은 이 문서에만 정의되어 있다.

**2. 검증 우선 원칙**

- `.py` 수정 후에는 반드시 `./bin/harness-check.sh`를 실행하여 검증한다.
- 비밀 정보를 포함할 가능성이 있는 파일(`.env`, 인증 정보 등)은 커밋하지 않는다.

**3. 커밋 스테이징**

- 현재 작업에 관련된 파일만 `git add <파일>`로 명시적으로 스테이징한다.
- 비밀 정보를 포함할 가능성이 있는 파일(`.env`, 인증 정보 등)은 커밋하지 않는다.

**4. 임시 스크립트 및 일회성 자동화 파일**

- 임시 또는 일회성 스크립트는 **항상** 프로젝트 루트의 **`tmp/`** 디렉터리에 생성한다.

**5. 언어 정책**

- 채팅 대화, 임시 응답 등 기타 출력은 사용자의 주 사용언어(한국어)로만 작성한다.
